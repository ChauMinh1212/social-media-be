pipeline {

    agent any

    stages {

        stage('Build with sdsdsd') {
            steps {
                echo 'Hello jenkins, mình tên Minh'
            }
        }
 
    }
    post {
        // Clean after build
        always {
            cleanWs()
        }
    }
}