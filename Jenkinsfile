pipeline {

    agent any

    stages {

        stage('Build with sdsdsd') {
            steps {
                echo 'Hello jenkins'
            }
        }
 
    }
    post {
        // Clean after build
        always {
            cleanWs()
        }
    }
}