pipeline {

    agent any

    stages {

        stage('Build with sdsdsd') {
            steps {
                echo 'Hello jenkins, mình tên Minh 1'
            }
        }
 
    }
    post {
        // Clean after build
        always {
            cleanWs()
        }
    }
}