pipeline {

    agent any

    stages {

        stage('Build with sdsdsd') {
            steps {
                echo 'Hello jenkins, mình tên Minh 2'
            }
        }
 
    }
    post {
        // Clean after build
        always {
            cleanWs()
        }
    }
}